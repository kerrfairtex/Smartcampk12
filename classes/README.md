# CLASSES

Since KerrFairtex 12.7, classes are autoloaded using a PSR-4 autoloader in the `Warehouse.php` file.

## File structure

`classes/[Namespace]/[Class].php`

## Instanciate a class

```php
$object = new \Namespace\Class( $argument );
```
